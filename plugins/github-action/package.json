{
  "name": "@ramen-ai/github-action",
  "version": "0.1.0",
  "description": "GitHub Action that scans pull request diffs for prompt/policy-relevant text changes, evaluates them against the ramen-ai PaaS API, and fails the CI build (with a cryptographically-receipted PR comment) on a BLOCKED verdict.",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "@ramen-ai/node-core": "file:../../core-clients/node"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
